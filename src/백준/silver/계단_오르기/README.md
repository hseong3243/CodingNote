## 핵심 포인트

- DP
- 바텀업을 방식의 계단 오르기 문제 풀이
- 현재의 계단을 처음으로 밟는 경우(`dp[i][0]`), 두 계단 이전의 처음 값(`dp[i-2][0]`)과 두번째인 값(`dp[i-2][1]`)을 비교하여 더 큰 값과 현재 계단의 값(`arr[i]`)를 더한다.
- 현재의 계단을 두번째로 밟는 경우(`dp[i][1]`), 한 계단 이전의 처음 값(`dp[i-1][0]`])과 현재 계단의 값(`arr[i]`)를 더한다.

## 문제 링크

https://www.acmicpc.net/problem/2579
